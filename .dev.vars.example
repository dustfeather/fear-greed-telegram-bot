# Example .dev.vars file for local development
# Copy this file to .dev.vars and fill in your actual values
# .dev.vars is gitignored and should never be committed

# Telegram Bot Token (from @BotFather)
TELEGRAM_BOT_TOKEN_SECRET=your_telegram_bot_token_here

# Telegram Webhook Secret (for verifying webhook requests)
# Generate a secure random string (e.g., using openssl rand -hex 32)
# This secret must match between webhook setup and your Worker environment
TELEGRAM_WEBHOOK_SECRET=your_webhook_secret_here

# Admin Chat ID for error notifications
# Get your chat ID by sending a message to your bot and checking the update
ADMIN_CHAT_ID=your_admin_chat_id_here

# KV Namespace IDs (optional - only needed if testing KV locally)
FEAR_GREED_KV_NAMESPACE_ID=your_production_kv_namespace_id
FEAR_GREED_KV_PREVIEW_ID=your_preview_kv_namespace_id

FEAR_GREED_D1_DATABASE_ID=your_d1_id

# Worker URL (optional - only needed for local webhook setup)
WEBHOOK_URL=https://your-worker-name.your-subdomain.workers.dev
